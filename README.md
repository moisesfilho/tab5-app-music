# Tab5 Music Player App (`tab5-app-music`)

Aplicativo isolado de reprodução de música para o **Tab5 OS**.

## Compilação e Empacotamento

```bash
chmod +x tools/build.sh
./tools/build.sh
```

O pacote `.tab5pkg` será gerado em `dist/com.tab5.music.tab5pkg`.
